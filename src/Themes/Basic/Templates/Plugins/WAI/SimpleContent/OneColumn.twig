<div class="container">
  <div class="row">
    <div class="col-12">
      {% if heading %}
        <h{{ headingLevel }}>{{ heading }}</h{{ headingLevel }}>
      {% endif %}
      {{ content|raw }}
    </div>
  <div>
</div>

