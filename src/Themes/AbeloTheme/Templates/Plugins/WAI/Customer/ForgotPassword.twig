 <!-- login area start -->
  <div class="login-register-area mb-50px mt-40px">
    <div class="container">
      <div class="row">
        <div class="col-lg-9 col-md-12 ml-auto mr-auto">
          <div class="login-register-wrapper">
            <div class="login-register-tab-list nav">
              <a class="active" data-toggle="tab" href="#lg1">
                <h4>{{ translate("Lost password", "Customer") }}</h4>
              </a>
            </div>
            <div class="tab-content">
              <div id="lg1" class="tab-pane active">
                <div class="login-form-container">
                  <div class="login-register-form">
                    {% if user.changePasswordError %}
                      <div class="notification mb-3">
                        <p class="error">{{ user.changePasswordError }}</p>
                      </div>
                    {% endif %}
                    {% if status == "OK" %}
                      <form method="post" id="forgotPasswordForm">
                        <input type="hidden" name="forgotPassword" value="1" />
                        <input type="password" 
                          name="password" 
                          placeholder="{{ translate("New Password", "Customer") }}"
                          {% if user.changePasswordError %} class="required-empty" {% endif %} 
                          required 
                          />
                        <div class="button-box">
                          <div class="login-toggle-btn">
                            <button 
                              type="submit"
                              onclick="$('#forgotPasswordForm').submit();"
                            ><span>{{ translate("Change password", "Customer") }}</span></button>
                          </div>
                        </div>
                      </form>
                    {% else %}
                      <div id='unknownAccount' style="display:none" class="mb-3">
                        <div class="notification">
                          <p class="error">{{ translate("Account doesn't exists", "Customer") }}</p>
                        </div>
                      </div>
                      <div id='emailIsEmpty' style="display:none" class="mb-3">
                        <div class="notification">
                          <p class="error">{{ translate("Empty required field email", "Customer") }}</p>
                        </div>
                      </div>
                      <div id='emailIsInvalid' style="display:none" class="mb-3">
                        <div class="notification">
                          <p class="error">{{ translate("Invalid email", "Customer") }}</p>
                        </div>
                      </div>
                      <div id='accountIsNotValidated' style="display:none" class="mb-3">
                        <div class="notification">
                          <p class="error">{{ translate("The account must be verified first", "Customer") }}</p>
                        </div>
                      </div>

                      <div id="forgotPasswordDiv" class="row">
                        <div class="col-lg-9">
                          <form method="post" id="forgotPasswordForm">
                            <input 
                              type="text" 
                              name="email" 
                              placeholder="{{ translate("Your email", "Customer") }}" 
                              required 
                              />
                          </form>
                        </div>
                        <div class="col-lg-3">
                          <div class="button-box mt-1" style="text-align:center;">
                            <button 
                              type="submit"
                              onclick="AbeloThemeCustomer.forgotPassword();"
                            ><span>{{ translate("Submit", "Customer") }}</span></button>
                          </div>
                        </div>
                      </div>
                    {% endif %}
                    <div id="recoveryPasswordSent" class="notification" style="display:none;">
                      <p class="success">✓ {{ translate("A password recovery link was successfully sent to your mail account.", "Customer") }}</p>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
<!-- login area end -->